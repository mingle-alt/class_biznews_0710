{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "prisma generate"
  },
  "deploy": {
    "startCommand": "npm run feeds:fetch",
    "restartPolicyType": "NEVER"
  }
}
